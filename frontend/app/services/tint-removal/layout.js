export const metadata = {
  title: "Professional Tint Removal | Lux Customs Melbourne",
  description: "Safe and clean window tint removal services in Melbourne. Remove bubbling, purple, or old window films without damaging your vehicle's glass or defroster lines.",
};

export default function TintRemovalLayout({ children }) {
  return (
    <section className="min-h-screen bg-zinc-950">
      {children}
    </section>
  );
}